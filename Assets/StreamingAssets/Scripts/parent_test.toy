print "running...";

import "Unity";

var root = Unity.FetchGameObject("Root");

var child1 = Unity.LoadGameObject("EmptyChild");
var child2 = Unity.LoadGameObject("EmptyChild");
var child3 = Unity.LoadGameObject("EmptyChild");

child1.Transform.SetParent(root.Transform, false);
child2.Transform.SetParent(root.Transform, false);
child3.Transform.SetParent(root.Transform, false);

print child1.Transform.IsChildOf(root.Transform); //true
print child2.Transform.IsChildOf(child1.Transform); //false

print root.Transform.GetChildCount();

root.Transform.GetChild(1).SetParent(null, false);

root.Behaviour.Awake = () => {
	print "Awake";
};

root.Behaviour.Update = () => {
	if (Unity.GetButtonDown("Fire1")) {
		Unity.LoadScene("GamePlay", "Single");
	}
};